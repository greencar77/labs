Try different LockModeType values.

Steps/Questions
-
1) `LockModeType.OPTIMISTIC` 
2) `LockModeType.OPTIMISTIC_FORCE_INCREMENT` 
3) What if `LockModeType.OPTIMISTIC` is used without version attribute? (QC5A) 

_id:VDB5DL
_title:LockModeType - Optimistic cases