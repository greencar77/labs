Create simple Spring Boot application using Spring Data JPA and Hibernate.

Create two tables CUSTOMER and ORDER (be cautious with the naming, "ORDER" is reserved word in SQL).

Use `EntityManager` for db access.

Implement and execute simple calls to every CRUD action.

Steps/Questions
-
1) Load some data using db initializer script (Q07M)
2) Implement creation (C)
3) Implement query (R)
4) Implement update (U)
5) Implement delete (D)
6) Configure the application to log db statements (QRXW)
7) What happens if `@Transactional` is removed from data fetching methods (Service and Repository)? Is functionality still working? (Q7R4)
8) What happens if `@Transactional` is removed from data update methods (Service and Repository)? Is functionality still working? (QFLR)

_id:G14DCI
_title:Spring Data EntityManager