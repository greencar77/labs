Try "instanceof" pattern matching feature.

Use Java 14.

Questions
=
1) Try simple case of `instanceof`.
2) Try the case where variable setting and usage is combined in one "if" clause.
3) Try the case with negative clause like `if (!(obj instanceof String s)) {`.

Resources:
* [JEP 305](https://openjdk.org/jeps/305)
* [Baeldung - Pattern Matching for instanceof in Java](https://www.baeldung.com/java-pattern-matching-instanceof)

_id:VV48J0