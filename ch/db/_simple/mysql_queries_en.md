Create SQL queries in MySQL sample database 'sakila'.

Generated by ChatGPT.

**Exercise 1: Top Customers by Rentals**

Task:
Find the 10 customers who have rented the most films.

Requirements:

* Show:
  * Customer first name
  * Customer last name
  * Number of rentals
* Sort by rental count (highest first).

Expected tables:
* `customer`
* `rental`

Hint:
Use `COUNT()`, `GROUP BY`, and `ORDER BY`.


**Exercise 2: Films Never Rented**

Task:
Find all films that have never been rented.

Requirements:

* Show:
  * Film title
  * Release year
  * Rental rate

Expected tables:
* film
* inventory
* rental

Hint:
Think about a `LEFT JOIN` and checking for `NULL`.


**Exercise 3: Revenue by Category**

Task:
Calculate the total revenue generated by each film category.

Requirements:

Display:

* Category name
* Number of rentals
* Total revenue

Sort by revenue descending.

Expected tables:
* `category`
* `film_category`
* `inventory`
* `rental`
* `payment`

Hint:
You'll need several joins.


**Exercise 4: Actors in the Most Films**

Task:
Find the 5 actors who appear in the most films.

Requirements:

Display:

* Actor full name
* Number of films

Sort descending.

Expected tables:
* `actor`
* `film_actor`

Hint:
Use `COUNT()` with `GROUP BY`.


**Exercise 5: Customers Who Rented From Multiple Stores**

Task:
Find customers who have rented films from both stores.

Requirements:

Display:
* Customer name
* Number of distinct stores rented from

Only include customers who rented from more than one store.

Expected tables:
* `customer`
* `rental`
* `inventory`
* `store`

Hint:
Use `COUNT(DISTINCT ...)`.


**Bonus Challenge**

Task:
For each customer, determine their favorite film category (the category from which they rented the most films).

Requirements:

Display:

* Customer name
* Favorite category
* Number of rentals in that category

If multiple categories tie for first place, return all of them.

Steps/Questions
-
1) Exercise 1: Top Customers by Rentals (QQ3J)
2) Exercise 2: Films Never Rented (QATM)
3) Exercise 3: Revenue by Category (QHMT)
4) Exercise 4: Actors in the Most Films (Q0ZA)
5) Exercise 5: Customers Who Rented From Multiple Stores (QTG7)
6) Bonus Challenge (Q3N7)

Resources
-
* [sakila db ER](https://dev.mysql.com/doc/workbench/en/wb-documenting-sakila.html)

_id:AJ25KW
_title:MySQL queries
_tag:db,sql,db_mysql,sakila